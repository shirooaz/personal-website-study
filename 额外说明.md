# 额外说明：网页自定义指南

---

## 一、背景更换方法

### 1. 纯色背景

在 `style.css` 中找到 `.page-bg`，修改 `background` 属性：

```css
.page-bg {
    /* 当前墨绿色主题背景 */
    background: radial-gradient(ellipse at 20% 20%, rgba(125, 211, 168, 0.08) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 80%, rgba(199, 125, 104, 0.05) 0%, transparent 50%);
    /* 改成你喜欢的纯色：*/
    background: #0a1a14;
}
```

### 2. 渐变背景

```css
.page-bg {
    background: linear-gradient(135deg, #0a1a14 0%, #142b1f 50%, #0d2118 100%);
}
```

### 3. 星空背景

```css
.page-bg {
    background: 
        radial-gradient(2px 2px at 20px 30px, #eee, transparent),
        radial-gradient(2px 2px at 40px 70px, rgba(255,255,255,0.8), transparent),
        radial-gradient(2px 2px at 50px 160px, rgba(255,255,255,0.6), transparent),
        radial-gradient(2px 2px at 90px 40px, #fff, transparent),
        radial-gradient(2px 2px at 130px 80px, rgba(255,255,255,0.7), transparent),
        radial-gradient(2px 2px at 160px 120px, #fff, transparent),
        #0a1a14;
    background-size: 200px 200px;
}
```

---

## 二、主题颜色自定义

### 1. 暗色主题颜色（当前）

```css
:root {
    --bg-primary: #0a1a14;      /* 主背景 - 深邃墨绿 */
    --bg-secondary: #0f2319;    /* 次级背景 */
    --bg-card: #142b1f;         /* 卡片背景 */
    --text-primary: #d4e8dc;    /* 主文字 - 柔和薄荷白 */
    --text-secondary: #8bc9a0;  /* 次级文字 - 清新薄荷绿 */
    --text-muted: #5a7a66;      /* 弱化文字 */
    --accent-primary: #f78c6c;  /* 强调色 - 珊瑚橙 */
    --accent-secondary: #e06c75;/* 次强调色 - 柔粉 */
}
```

### 2. 浅色主题颜色

```css
[data-theme="light"] {
    --bg-primary: #faf8f5;      /* 暖白背景 */
    --bg-secondary: #f5f1eb;    /* 米白次级 */
    --bg-card: #ffffff;         /* 纯白卡片 */
    --text-primary: #2d3a35;    /* 深绿文字 */
    --text-secondary: #5a6e64;  /* 次级文字 */
    --accent-primary: #f78c6c;  /* 强调色 - 珊瑚橙 */
}
```

### 3. 自定义强调色

```css
/* 橙粉渐变（当前默认） */
--accent-gradient: linear-gradient(135deg, #f78c6c, #e06c75, #c084fc);

/* 紫蓝渐变 */
--accent-gradient: linear-gradient(135deg, #667eea, #764ba2);

/* 绿橙渐变 */
--accent-gradient: linear-gradient(135deg, #11998e, #38ef7d);

/* 日落渐变 */
--accent-gradient: linear-gradient(135deg, #ff7e5f, #feb47b);
```

---

## 三、Logo Emoji 自定义

在 `script.js` 中找到 `LOGO_EMOJIS` 数组：

```javascript
const LOGO_EMOJIS = [
    '🌿', '🍃', '🌸', '🌺', '🦋', '🐦', '📚', '🎋', 
    '✨', '🌱', '🪴', '🌾', '🍀', '🌴', '🎍'
];
```

### 扩展更多emoji

```javascript
const LOGO_EMOJIS = [
    '🌿', '🍃', '🌸', '🌺', '🌻', '🌼', '🌷',  // 植物花卉
    '🦋', '🐦', '🐰', '🦊', '🐱',               // 动物
    '📚', '📖', '✏️', '📝',                       // 学习
    '🎋', '🎍', '🌱', '🪴', '🌾',                 // 竹与绿植
    '✨', '💫', '⭐', '🌙', '☀️',                 // 星星与光
    '💭', '🎵', '☕', '🍵',                       // 生活
];
```

---

## 四、樱花效果自定义

在 `script.js` 中找到樱花配置：

```javascript
const CONFIG = {
    sakuraCount: 15,        // 樱花数量
    fireflyCount: 20,       // 萤火虫数量
    // ...
};
```

### 樱花颜色（当前）

```javascript
const sakuraColors = [
    'rgba(255, 183, 197, 0.8)',  // 粉樱
    'rgba(255, 192, 203, 0.7)',  // 淡粉
    'rgba(255, 218, 233, 0.6)',  // 极淡粉
];
```

### 修改为其他颜色

```javascript
const sakuraColors = [
    'rgba(135, 206, 235, 0.8)',  // 天蓝
    'rgba(176, 224, 230, 0.7)',  // 淡蓝
    'rgba(255, 255, 240, 0.6)',  // 米白
    'rgba(230, 230, 250, 0.6)',  // 薰衣草
];
```

---

## 五、萤火虫效果自定义

### 萤火虫颜色（当前）

```javascript
const fireflyColors = [
    'rgba(200, 255, 150, 0.9)',  // 萤绿
    'rgba(150, 255, 200, 0.8)',  // 淡绿
    'rgba(180, 255, 180, 0.7)',  // 浅绿
];
```

### 修改为其他颜色

```javascript
const fireflyColors = [
    'rgba(255, 255, 150, 0.9)',  // 暖黄
    'rgba(255, 215, 0, 0.8)',     // 金色
    'rgba(255, 255, 200, 0.7)',   // 淡黄
];
```

---

## 六、导航链接功能

### 当前导航结构

```html
<nav class="nav">
    <div class="nav-container">
        <a href="#home" class="nav-logo">...</a>  <!-- 首页锚点 -->
        <a href="#articles" class="nav-link">📚 文章</a>   <!-- 滚动到文章区域 -->
        <a href="#about" class="nav-link">💭 关于</a>      <!-- 滚动到关于区域 -->
        <a href="#guestbook" class="nav-link">💬 留言板</a><!-- 滚动到留言板 -->
    </div>
</nav>
```

### 添加新链接

```html
<a href="#new-section" class="nav-link">🆕 新功能</a>
```

然后在页面中添加对应id的部分：

```html
<section id="new-section" class="section">
    <!-- 内容 -->
</section>
```

---

## 七、留言板功能

### API 配置

留言板使用远程 API + 本地存储的混合模式：

```javascript
const MESSAGE_API = 'https://api.153904.xyz/api/messages';
```

### 本地存储键名

```javascript
const STORAGE_KEY = 'qiufeng_guestbook';
```

### 工作流程

1. 提交留言 → 尝试发送到远程 API
2. API 失败时 → 保存到 localStorage
3. 读取留言 → 优先从 API 获取，失败时读取本地存储

---

## 八、打字机效果文本

在 `script.js` 中找到：

```javascript
const phrases = [
    '记录成长的每一步 ✨',
    '分享技术与生活的点滴 💭',
    '二次元 · 生机 · 静谧 🌿',
    '欢迎来到我的世界 🏠',
];
```

### 自定义短语

```javascript
const phrases = [
    '你的第一句话 ✨',
    '第二句话 💡',
    '第三句话 🎯',
];
```

---

## 九、社交链接

在 `script.js` 中找到：

```javascript
const socialLinks = [
    { name: 'GitHub', url: 'https://github.com/shirooaz', icon: '🐱' },
    { name: 'Gitee', url: 'https://gitee.com/shirooaz', icon: '🐼' },
];
```

### 添加新链接

```javascript
const socialLinks = [
    { name: 'Twitter', url: 'https://twitter.com/yourname', icon: '🐦' },
    { name: 'Bilibili', url: 'https://bilibili.com/yourname', icon: '📺' },
];
```

---

## 十、滚动动画

### 修改触发动画距离

```javascript
const CONFIG = {
    scrollThreshold: 100,  // 滚动100px后显示
    // ...
};
```

### 修改动画延迟

```javascript
// 元素依次出现，每隔100ms
<div class="reveal" style="--delay: 0.1s">内容1</div>
<div class="reveal" style="--delay: 0.2s">内容2</div>
```

---

*文档更新时间：2026-06-12*